<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Cinzel+Decorative:wght@700&family=Exo+2:wght@300;400;700&family=Crimson+Pro:italic,wght@0,400;1,600&display=swap');

        :root {
            --yang-color: #00f2ff; /* Ciano Elétrico */
            --yin-color: #ff00ff;  /* Magenta/Rosa Fenda */
            --bg-dark: #0a0a0c;
            --text-main: #e0e0e0;
            --accent-gold: #ffd700;
        }

        body {
            background-color: #111;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            perspective: 1000px;
        }

        /* Container da Carta com Efeito 3D */
        .power-card {
            width: 480px;
            background: rgba(10, 10, 12, 0.95);
            border: 2px solid transparent;
            border-image: linear-gradient(45deg, var(--yang-color), var(--yin-color)) 1;
            border-radius: 15px;
            padding: 20px;
            color: var(--text-main);
            font-family: 'Exo 2', sans-serif;
            position: relative;
            box-shadow: 0 0 30px rgba(0, 0, 0, 0.8), 0 0 15px rgba(255, 0, 255, 0.2);
            overflow: hidden;
            transition: transform 0.1s ease-out;
            transform-style: preserve-3d;
        }

        /* Efeito de Scanline/Glitch no fundo */
        .power-card::before {
            content: " ";
            position: absolute;
            top: 0; left: 0; bottom: 0; right: 0;
            background: linear-gradient(rgba(18, 16, 16, 0) 50%, rgba(0, 0, 0, 0.25) 50%), 
                        linear-gradient(90deg, rgba(255, 0, 0, 0.06), rgba(0, 255, 0, 0.02), rgba(0, 0, 255, 0.06));
            background-size: 100% 4px, 3px 100%;
            z-index: 10;
            pointer-events: none;
        }

        /* Título com Animação Glitch */
        .header {
            text-align: center;
            border-bottom: 2px solid rgba(255, 255, 255, 0.1);
            padding-bottom: 10px;
            margin-bottom: 15px;
        }

        .title {
            font-family: 'Cinzel Decorative', serif;
            font-size: 24px;
            letter-spacing: 2px;
            text-transform: uppercase;
            margin: 0;
            position: relative;
            color: #fff;
            text-shadow: 2px 0 var(--yang-color), -2px 0 var(--yin-color);
            animation: glitch 3s infinite;
        }

        @keyframes glitch {
            0% { transform: translate(0); }
            2% { transform: translate(-2px, 2px); }
            4% { transform: translate(2px, -2px); }
            6% { transform: translate(0); }
        }

        /* Grid de Informações Rápidas */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 10px;
            font-size: 0.85em;
            background: rgba(255, 255, 255, 0.03);
            padding: 10px;
            border-radius: 8px;
            margin-bottom: 20px;
        }

        .stat-item b { color: var(--accent-gold); }

        /* Estilo das Seções Yin e Yang */
        .mode-container {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        .mode-box {
            padding: 15px;
            border-radius: 8px;
            background: rgba(0, 0, 0, 0.4);
            border-left: 4px solid;
            position: relative;
        }

        .yang-box { border-color: var(--yang-color); box-shadow: -5px 0 15px rgba(0, 242, 255, 0.1); }
        .yin-box { border-color: var(--yin-color); box-shadow: -5px 0 15px rgba(255, 0, 255, 0.1); }

        .mode-title {
            font-weight: bold;
            font-size: 1.1em;
            margin-bottom: 8px;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .flavor-text {
            font-family: 'Crimson Pro', serif;
            font-style: italic;
            font-size: 0.95em;
            color: #aaa;
            margin-bottom: 10px;
            display: block;
        }

        .rule-list {
            margin: 0;
            padding-left: 18px;
            font-size: 0.9em;
            line-height: 1.4;
        }

        .rule-list li { margin-bottom: 5px; }

        /* Destaque para Status/Limitações */
        .tag {
            background: rgba(255, 255, 255, 0.1);
            padding: 2px 6px;
            border-radius: 4px;
            font-size: 0.8em;
            color: var(--accent-gold);
            font-weight: bold;
        }

        .recoil { color: #ff4d4d; font-weight: bold; }
        .consent { color: #70e000; font-weight: bold; }

        /* Tabela de Custo de PE */
        .cost-table {
            width: 100%;
            margin-top: 20px;
            border-collapse: collapse;
            font-size: 0.8em;
            color: #888;
        }

        .cost-table td { padding: 4px; border-bottom: 1px solid rgba(255, 255, 255, 0.05); }
        .total-cost { color: #fff; font-weight: bold; text-align: right; font-size: 1.2em; padding-top: 10px; }

    </style>
</head>
<body>

<div class="power-card" id="card">
    <div class="header">
        <h1 class="title">Dualidade de Hanz</h1>
        <div style="font-size: 0.9em; color: #888; margin-top: 5px;">👤 Usuário: Lug (Anjo) | ⭐ NP 1</div>
    </div>

    <div class="stats-grid">
        <div class="stat-item"><b>MP:</b> 5</div>
        <div class="stat-item"><b>PE:</b> 15</div>
        <div class="stat-item"><b>Atrib:</b> SAB [5]</div>
        <div class="stat-item"><b>Cast:</b> Padrão</div>
        <div class="stat-item"><b>Alvo:</b> Toque</div>
        <div class="stat-item"><b>Cat:</b> Ofen/Sup</div>
    </div>

    <div class="mode-container">
        <!-- MODO YANG -->
        <div class="mode-box yang-box">
            <div class="mode-title" style="color: var(--yang-color);">
                ⚔️ MODO YANG: O SELO DE LYAN
            </div>
            <span class="flavor-text">"A herança de guerra de Lyan Hanz, refinada pela análise de fendas." [3, 6]</span>
            <ul class="rule-list">
                <li><span class="tag">ANÁLISE</span> Teste <b>SAB (CD 12)</b> sorteia atributo alvo (1d6) [7].</li>
                <li><span class="tag">TEMPORAL</span> Cada acerto reduz o atributo em <b>-1</b> [8].</li>
                <li><span class="tag">SELAMENTO</span> Se o atributo chegar a 1, alvo fica <b>Paralisado</b> [9].</li>
                <li><span class="recoil">3ª LEI DE NEWTON:</span> Erros causam <b>5% do seu HP Máx</b> em recuo.</li>
            </ul>
        </div>

        <!-- MODO YIN -->
        <div class="mode-box yin-box">
            <div class="mode-title" style="color: var(--yin-color);">
                ✿ MODO YIN: O RESPIRO DE URO
            </div>
            <span class="flavor-text">"A paz das estufas de Uro Tomiawa, onde a vida flui em harmonia." [3, 10]</span>
            <ul class="rule-list">
                <li><span class="consent">VÍNCULO CONSENSUAL:</span> Requer aceitação voluntária do aliado.</li>
                <li><span class="tag">INVERSÃO</span> Converte <b>HP em MP</b> (proporção 1:1) entre usuários [11].</li>
                <li><span class="tag">FLUXO</span> Redistribui pontos de atributos entre Lug e o alvo.</li>
                <li><span class="tag">CALMARIA</span> Pós-uso: <b>10 cenas</b> sem habilidades ofensivas [9].</li>
            </ul>
        </div>
    </div>

    <table class="cost-table">
        <tr><td>Núcleos (Dano + Buff + Transfer)</td><td align="right">9 PE</td></tr>
        <tr><td>Descritores (Temporal + Inversão + Análise)</td><td align="right">28 PE</td></tr>
        <tr><td>Duração & Status (10 turnos + Vulnerável)</td><td align="right">25 PE</td></tr>
        <tr><td>Limitações (Preparação, Custo HP, Consentimento, Exclusão)</td><td align="right">-47 PE</td></tr>
        <tr><td colspan="2" class="total-cost">CUSTO FINAL: 15 PE</td></tr>
    </table>
</div>

<script>
    const card = document.getElementById('card');
    const motionMax = 15; // Máximo de rotação em graus

    document.addEventListener('mousemove', (e) => {
        const x = e.clientX / window.innerWidth;
        const y = e.clientY / window.innerHeight;

        const rotateX = (y - 0.5) * motionMax * -1;
        const rotateY = (x - 0.5) * motionMax;

        card.style.transform = `rotateX(${rotateX}deg) rotateY(${rotateY}deg)`;
    });

    document.addEventListener('mouseleave', () => {
        card.style.transform = `rotateX(0deg) rotateY(0deg)`;
    });
</script>

</body>
</html>
